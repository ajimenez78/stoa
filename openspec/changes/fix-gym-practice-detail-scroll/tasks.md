## 1. Practice Detail ScrollContainer Node Hierarchy

- [x] 1.1 Wrap practice detail body content inside a `ScrollContainer` (`PracticeDetailScroll`) in `Dungeons/gym.tscn` and update exported node paths for `detail_title_label`, `detail_description_label`, `detail_meta_label`, `detail_steps_label`, and `complete_button`.
- [x] 1.2 Update `_configure_scroll_pass_through` or script references in `Dungeons/Scripts/gym.gd` to ensure mouse filter pass-through and touch dragging work properly for `PracticeDetailScroll`.

## 2. Verification

- [x] 2.1 Verify clean build using Godot Engine v4.7.1 headless runner without GDScript errors or broken node path warnings.
