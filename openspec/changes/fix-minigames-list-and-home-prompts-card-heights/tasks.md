## 1. Deferred Scaling Implementation

- [x] 1.1 Update `_rebuild_minigame_cards()` in `gym.gd` to use `call_deferred("_apply_font_scale")`.
- [x] 1.2 Update `_build_prompt_cards()` in `home.gd` to use `call_deferred("_apply_font_scale")`.

## 2. Verification

- [x] 2.1 Verify project builds cleanly in headless Godot mode with no errors or warnings.
